language: python
install:
    - pip install coveralls

python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "pypy"

script:
    - python setup.py -q nosetests

after_success:
    - coverage run --source=mistune setup.py -q nosetests
    - coveralls

notifications:
    email: false
